import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.studiomanager.pro',
  appName: 'Studio Manager Pro',
  webDir: 'dist'
};

export default config;
